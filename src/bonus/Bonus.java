package bonus;

public class Bonus {
//    Bonus Challenge:

//    Implement a parallelStream to process large data
//    Given a large list of integers, process it using parallelStream to find the sum of squares.
//
//            List<Integer> numbers = IntStream.range(1, 1000000).boxed().collect(Collectors.toList());
//
//    Find the longest word in a list of strings using Stream
//    Given a list of strings, find the longest word using Stream.
//
//    List<String> words = Arrays.asList("apple", "banana", "cherry", "pineapple");
//
//    Count occurrences of each character in a string
//    Given a string, count the occurrences of each character and return the result as a map.
//
//            String input = "abracadabra";



}
